# configs/central/m6_focal_high_gamma.yaml
# Milestone 6: Focal Loss with higher gamma (focus MORE on hard examples)

defaults:
  - m1_baseline

use_focal_loss: true
focal_gamma: 3.0  # Higher gamma = even more focus on hard misclassified examples
focal_alpha: "auto"
use_class_weights: false
use_thresholds: false

wandb:
  project: "e20-4yp-backdoor-resilient-federated-nids"
  group: "central_m6_focal_high_gamma"
  tags: []
