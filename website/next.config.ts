import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  basePath: "/e20-4yp-backdoor-resilient-federated-nids",
  images: { unoptimized: true },
};

export default nextConfig;
